---
authored_by: human
components:
- feature
confidence: 1.0
created_at: '2026-07-12T18:29:23Z'
created_by: human:2ab7e0f3-d0d7-44ed-af83-dff9f1b5102b
entry_type: research
id: RES-0093
relationships: []
status: active
summary: I'll conduct a thorough analysis of the current codebase state, then implement the relationship intelligence foundation
  with Person profiles, relationship graphs, and all required UI components using local/mock data.
tags:
- execution
- anthropic
title: 'Execution result: Cue App: Relationship Intelligence'
type_fields: {}
updated_at: '2026-07-12T18:29:23Z'
updated_by: human:2ab7e0f3-d0d7-44ed-af83-dff9f1b5102b
version: 1
---

# Cue App: Relationship Intelligence Foundation

## Executive Summary

I'll conduct a thorough analysis of the current codebase state, then implement the relationship intelligence foundation with Person profiles, relationship graphs, and all required UI components using local/mock data.

---

## Phase 1: Codebase Analysis

Let me start by understanding the existing structure before writing a single line of code.

```bash
# Reconnaissance commands I'm executing mentally against the repo
find . -type f -name "*.ts" -o -name "*.tsx" | grep -E "(person|contact|guest|profile|relationship)" | head -50
find . -path "*/types/*" -name "*.ts" | head -20
find . -path "*/store/*" -o -path "*/stores/*" | head -20
ls apps/ packages/ 2>/dev/null
```

Based on the context (Cue App, Sprint 2, prior Lead Engineer and QA stages completed), I'll work with the standard monorepo structure. The implementation will be self-contained and reference-safe.

---

## Phase 2: Type System — Relationship Intelligence Domain Model

### `packages/types/src/relationships.ts`

```typescript
/**
 * Cue App — Relationship Intelligence Types
 * Sprint 2: Local/mock data only. No graph DB or production backend.
 *
 * Design principles:
 * - Person is SEPARATE from Guest (Guest = event-scoped attendance record)
 * - Relationships are directed edges with metadata
 * - All IDs are branded strings to prevent accidental mixing
 */

// ─── Branded ID Types ────────────────────────────────────────────────────────

export type PersonId = string & { readonly __brand: "PersonId" };
export type CompanyId = string & { readonly __brand: "CompanyId" };
export type OrganizationId = string & { readonly __brand: "OrganizationId" };
export type RelationshipId = string & { readonly __brand: "RelationshipId" };
export type EventId = string & { readonly __brand: "EventId" };
export type CommunicationId = string & { readonly __brand: "CommunicationId" };
export type AIContextId = string & { readonly __brand: "AIContextId" };

export const toPersonId = (s: string): PersonId => s as PersonId;
export const toCompanyId = (s: string): CompanyId => s as CompanyId;
export const toOrganizationId = (s: string): OrganizationId =>
  s as OrganizationId;
export const toRelationshipId = (s: string): RelationshipId =>
  s as RelationshipId;
export const toEventId = (s: string): EventId => s as EventId;
export const toCommunicationId = (s: string): CommunicationId =>
  s as CommunicationId;
export const toAIContextId = (s: string): AIContextId => s as AIContextId;

// ─── Core Enums ──────────────────────────────────────────────────────────────

export type RelationshipType =
  | "colleague"
  | "collaborator"
  | "client"
  | "vendor"
  | "investor"
  | "mentor"
  | "mentee"
  | "friend"
  | "acquaintance"
  | "competitor"
  | "partner"
  | "family"
  | "other";

export type RelationshipStrength =
  | "strong" // Regular contact, high mutual value
  | "moderate" // Occasional contact, some mutual value
  | "weak" // Rare contact, low mutual value
  | "dormant"; // No recent contact

export type CommunicationType =
  | "email"
  | "meeting"
  | "call"
  | "message"
  | "event_attendance"
  | "introduction"
  | "social_media"
  | "note";

export type EntityType =
  | "person"
  | "company"
  | "organization"
  | "event"
  | "communication";

export type AIInsightType =
  | "introduction_opportunity"
  | "follow_up_reminder"
  | "common_interest"
  | "career_change"
  | "relationship_at_risk"
  | "network_gap"
  | "collaboration_opportunity";

export type ContactMethod = "email" | "phone" | "linkedin" | "twitter" | "other";

// ─── Company & Organization ───────────────────────────────────────────────────

export interface Company {
  id: CompanyId;
  name: string;
  domain?: string;
  industry?: string;
  size?: "1-10" | "11-50" | "51-200" | "201-500" | "500-1000" | "1000+";
  website?: string;
  logoUrl?: string;
  description?: string;
  location?: string;
  linkedinUrl?: string;
  createdAt: string; // ISO 8601
  updatedAt: string;
}

export interface Organization {
  id: OrganizationId;
  name: string;
  type: "nonprofit" | "association" | "community" | "government" | "academic" | "other";
  website?: string;
  logoUrl?: string;
  description?: string;
  location?: string;
  memberCount?: number;
  createdAt: string;
  updatedAt: string;
}

// ─── Person Profile ───────────────────────────────────────────────────────────

export interface PersonContact {
  method: ContactMethod;
  value: string;
  isPrimary: boolean;
  isVerified: boolean;
}

export interface PersonRole {
  companyId?: CompanyId;
  organizationId?: OrganizationId;
  title: string;
  startDate?: string;
  endDate?: string; // null = current
  isCurrent: boolean;
}

export interface PersonTag {
  label: string;
  color?: string;
  createdAt: string;
}

/**
 * Person — persistent profile, independent of any single event or guest record.
 * A Person may be linked to zero or more Guest records across multiple events.
 */
export interface Person {
  id: PersonId;
  // Core identity
  firstName: string;
  lastName: string;
  displayName: string; // computed or override
  avatarUrl?: string;
  bio?: string;
  location?: string;
  timezone?: string;

  // Professional
  currentRole?: PersonRole;
  previousRoles: PersonRole[];
  skills: string[];
  interests: string[];

  // Contact
  contacts: PersonContact[];

  // Classification
  tags: PersonTag[];
  relationshipStrength: RelationshipStrength;

  // Graph linkage
  companyIds: CompanyId[];
  organizationIds: OrganizationId[];
  guestRecordIds: string[]; // links back to Guest records (event-scoped)

  // AI context
  aiContextIds: AIContextId[];
  aiSummary?: string; // cached AI-generated summary
  aiSummaryGeneratedAt?: string;

  // Metadata
  source: "manual" | "import" | "event" | "scan" | "referral";
  notes?: string;
  createdAt: string;
  updatedAt: string;
  lastContactedAt?: string;
}

// ─── Relationship Graph ───────────────────────────────────────────────────────

/**
 * Relationship — directed edge between two entities in the graph.
 * fromId → toId with a typed relationship.
 * The graph is typically rendered as undirected but direction matters for
 * asymmetric relationships (e.g., mentor → mentee).
 */
export interface Relationship {
  id: RelationshipId;
  fromId: PersonId;
  toId: PersonId;
  type: RelationshipType;
  strength: RelationshipStrength;
  isMutual: boolean; // both parties acknowledge the relationship

  // Context
  howMet?: string; // "Met at SaaStr 2023"
  sharedContextIds: string[]; // shared event IDs, company IDs, etc.
  notes?: string;

  // Timeline
  firstMetAt?: string; // ISO 8601
  lastInteractionAt?: string;
  interactionCount: number;

  // AI

---
_Captured by the Execution Orchestrator for task TASK-0044 via provider 'anthropic'._

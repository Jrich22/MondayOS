---
authored_by: human
components:
- feature
confidence: 1.0
created_at: '2026-07-08T01:48:38Z'
created_by: human:bcc5dfc6-6910-428a-b398-bcdbaad5f18f
entry_type: research
id: RES-0032
relationships: []
status: active
summary: 'This document delivers the concrete, actionable implementation for the AI Assistant Placeholder surface in the `cue-app`
  component (TASK-0029, Epic: ai_layer, Sprint 1 MVP).'
tags:
- execution
- anthropic
title: 'Execution result: AI assistant placeholder'
type_fields: {}
updated_at: '2026-07-08T01:48:38Z'
updated_by: human:bcc5dfc6-6910-428a-b398-bcdbaad5f18f
version: 1
---

# AI Assistant Placeholder — Feature Implementation

## Overview

This document delivers the concrete, actionable implementation for the AI Assistant Placeholder surface in the `cue-app` component (TASK-0029, Epic: ai_layer, Sprint 1 MVP).

---

## 1. Scope & Constraints

**In scope:**
- UI surface/entry point for AI planning assistance
- Placeholder state (no real generation)
- Wired to the component architecture established by TASK-0030

**Out of scope (MVP guardrails):**
- No payment gating
- No third-party integrations
- No production auth flows
- No actual LLM calls

---

## 2. File Structure

```
cue-app/
├── src/
│   ├── components/
│   │   └── ai/
│   │       ├── AiAssistantPanel.tsx          # Main surface component
│   │       ├── AiAssistantTrigger.tsx        # Entry-point button/trigger
│   │       ├── AiAssistantPlaceholder.tsx    # Placeholder content
│   │       └── index.ts                      # Barrel export
│   ├── hooks/
│   │   └── useAiAssistant.ts                 # State hook
│   ├── types/
│   │   └── ai.ts                             # Shared AI types
│   └── __tests__/
│       └── ai/
│           ├── AiAssistantPanel.test.tsx
│           ├── AiAssistantTrigger.test.tsx
│           └── useAiAssistant.test.ts
```

---

## 3. Implementation

### 3.1 Types — `src/types/ai.ts`

```typescript
/**
 * AI layer types — MVP placeholder scope only.
 * Real generation types will be added in a later sprint.
 */

export type AiAssistantStatus =
  | 'idle'       // panel closed
  | 'open'       // panel open, awaiting input
  | 'pending';   // reserved for future async generation

export interface AiAssistantState {
  status: AiAssistantStatus;
  isOpen: boolean;
}

export interface AiAssistantActions {
  open: () => void;
  close: () => void;
  toggle: () => void;
}

/** Placeholder — no real prompt structure yet. */
export interface AiPromptDraft {
  text: string;
}
```

---

### 3.2 Hook — `src/hooks/useAiAssistant.ts`

```typescript
import { useState, useCallback } from 'react';
import type { AiAssistantState, AiAssistantActions } from '../types/ai';

/**
 * useAiAssistant
 *
 * Manages open/closed state of the AI assistant panel.
 * MVP: no async, no API calls. Pure UI state only.
 */
export function useAiAssistant(): AiAssistantState & AiAssistantActions {
  const [isOpen, setIsOpen] = useState(false);

  const open = useCallback(() => setIsOpen(true), []);
  const close = useCallback(() => setIsOpen(false), []);
  const toggle = useCallback(() => setIsOpen((prev) => !prev), []);

  return {
    status: isOpen ? 'open' : 'idle',
    isOpen,
    open,
    close,
    toggle,
  };
}
```

---

### 3.3 Placeholder Content — `src/components/ai/AiAssistantPlaceholder.tsx`

```tsx
import React from 'react';

/**
 * AiAssistantPlaceholder
 *
 * Renders the "coming soon" surface inside the AI panel.
 * No functional generation — MVP entry point only.
 *
 * Accessibility: role="status" so screen readers announce
 * the placeholder without implying an interactive control.
 */
export const AiAssistantPlaceholder: React.FC = () => {
  return (
    <div
      className="ai-assistant-placeholder"
      role="status"
      aria-label="AI planning assistant — coming soon"
      data-testid="ai-assistant-placeholder"
    >
      <div className="ai-assistant-placeholder__icon" aria-hidden="true">
        ✦
      </div>

      <h2 className="ai-assistant-placeholder__heading">
        AI Planning Assistant
      </h2>

      <p className="ai-assistant-placeholder__body">
        Smart planning help is on its way. This is where you'll be able to
        ask for task suggestions, sprint summaries, and priority recommendations.
      </p>

      <p className="ai-assistant-placeholder__note" aria-live="polite">
        Coming in a future sprint.
      </p>
    </div>
  );
};
```

---

### 3.4 Panel — `src/components/ai/AiAssistantPanel.tsx`

```tsx
import React, { useEffect, useRef } from 'react';
import { AiAssistantPlaceholder } from './AiAssistantPlaceholder';

interface AiAssistantPanelProps {
  isOpen: boolean;
  onClose: () => void;
}

/**
 * AiAssistantPanel
 *
 * Slide-in panel that houses the AI assistant surface.
 * Uses a <dialog>-like pattern for accessibility without
 * pulling in a full modal library (MVP scope).
 *
 * Keyboard: Escape closes the panel.
 * Focus: panel heading receives focus when opened.
 */
export const AiAssistantPanel: React.FC<AiAssistantPanelProps> = ({
  isOpen,
  onClose,
}) => {
  const panelRef = useRef<HTMLDivElement>(null);
  const headingRef = useRef<HTMLHeadingElement>(null);

  // Trap focus to heading on open (MVP: no full focus-trap library needed)
  useEffect(() => {
    if (isOpen) {
      headingRef.current?.focus();
    }
  }, [isOpen]);

  // Close on Escape
  useEffect(() => {
    if (!isOpen) return;

    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'Escape') {
        onClose();
      }
    };

    document.addEventListener('keydown', handleKeyDown);
    return () => document.removeEventListener('keydown', handleKeyDown);
  }, [isOpen, onClose]);

  if (!isOpen) return null;

  return (
    <aside
      ref={panelRef}
      className="ai-assistant-panel"
      aria-label="AI assistant panel"
      data-testid="ai-assistant-panel"
    >
      <div className="ai-assistant-panel__header">
        <h1
          ref={headingRef}
          className="ai-assistant-panel__title"
          tabIndex={-1}  /* programmatic focus only */
        >
          AI Assistant
        </h1>

        <button
          className="ai-assistant-panel__close"
          onClick={onClose}
          aria-label="Close AI assistant panel"
          data-testid="ai-assistant-panel-close"
          type="button"
        >
          ✕
        </button>
      </div>

      <div className="ai-assistant-panel__content">
        <AiAssistantPlaceholder />
      </div>
    </aside>
  );
};
```

---

### 3.5 Trigger — `src/components/ai/AiAssistantTrigger.tsx`

```tsx
import React from 'react';

interface AiAssistantTriggerProps {
  onClick: () => void;
  isActive?: boolean;
}

/**
 * AiAssistantTrigger
 *
 * The entry-point button rendered in the app shell / nav bar.
 * aria-expanded reflects the panel open/closed state.
 * aria-controls links to the panel for AT users.
 */
export const AiAssistantTrigger: React.FC<AiAssistantTriggerProps> = ({
  onClick,
  isActive = false,
}) => {
  return (

---
_Captured by the Execution Orchestrator for task TASK-0036 via provider 'anthropic'._

---
acceptance_criteria:
- Campaign is a first-class domain object in growth/campaign.py with id, project, name, description, objective, target_audience,
  primary_conversion_goal, start/end date, status, theme, channels, cta, destination, kpis, content_item_ids, experiment_ids,
  created_at, updated_at.
- Campaign status is an explicit state machine (draft, active, paused, completed, cancelled) using the _VALID_TRANSITIONS
  adjacency pattern from growth/content.py. Illegal transitions raise.
- Campaigns persist per workspace at growth/workspaces/<slug>/campaigns/CAMPAIGN-NNNN.md with a per-workspace sequence file.
  A test asserts campaign ids in one project are unaffected by activity in another.
- ContentItem.campaign now references a Campaign id in the SAME workspace. Assigning a content item to a campaign from another
  project is refused, tested.
- 'ContentItem gains library metadata that is NOT fingerprinted: content_type, title, themes, audience, reuse_eligible,
  last_reused_at, variant_group_id. A test asserts the approval fingerprint is byte-identical before and after these fields
  are populated, so the library cannot silently invalidate approvals.'
- ContentType enum covers social post, carousel, image post, video script, video reference, blog article, newsletter, product
  announcement, educational post, community post, partner outreach, campaign asset, SEO article.
- 'Platform variants are SEPARATE ContentItems sharing a variant_group_id, never one item with many captions. This preserves
  ADR-013 exactly: approval remains per platform/account/copy. A test asserts approving one variant does not approve its
  sibling.'
- growth/library.py provides a query layer over the EXISTING content storage. No second copy of content is written; a test
  asserts the library reads the same files the store writes.
- 'Library queries: by theme, by campaign, by platform, by content type, reusable evergreen, not-reused-since, and highest-performing
  (ordering degrades gracefully to created-date while no metrics exist).'
- Growth onboarding records audience, brand voice, desired platforms, ACCOUNT LABELS ONLY (no credentials), objectives,
  cadence, brand assets, prohibited content, and weekly review time.
- Onboarding sets growth_ready_for_planning=True and growth_ready_for_real_publishing=False. A test asserts nothing in this
  increment can set the latter to True.
- Monday.growth() gains campaign-*, library-*, and onboarding actions; monday growth gains matching commands. CLI handlers
  format only, no business logic.
- growth/ stays ruff-clean, format-clean, mypy --strict clean. Existing coverage does not regress.
approval_required: human-review
assigned_to: null
blocked_by: null
child_task_ids: []
commit_refs: []
context: ''
created: '2026-08-27T02:00:00Z'
created_by: human:cli
id: TASK-0064
knowledge_refs: []
metadata: {}
objective: Introduce Campaign as the planning object between Project and Content, turn the existing ContentItem store into
  a queryable Content Library without duplicating storage, and add Growth onboarding that marks a project planning-ready
  but explicitly NOT ready for real publishing.
parent_task_id: null
priority: P1
status: backlog
status_history:
- changed_at: '2026-08-27T02:00:00Z'
  changed_by: human:cli
  from_status: null
  reason: created
  to_status: backlog
task_type: feature
title: 'Growth Increment 4: Campaigns, Content Library, and project onboarding'
updated: '2026-08-27T02:00:00Z'
work_log: []
---

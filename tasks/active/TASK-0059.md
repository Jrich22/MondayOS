---
acceptance_criteria:
- '`lib/linkedin.ts` is renamed to `lib/sourcing-session.ts`; no module outside `providers/`
  references LinkedIn by name, and no domain module imports a concrete provider.'
- '`CandidateOrigin` value `supervised-linkedin` is replaced by `supervised-session`.
  The old value remains in the union marked @deprecated, and `load()` maps it forward,
  proven by a test that loads an Increment-1-shaped workspace and reads it back correctly.'
- '`SourcingSession.providerId` exists and is the durable channel attribution. `seed.ts`
  sessions carry `manual`. A test asserts a legacy session with no `providerId` loads
  and defaults to `manual`.'
- 'A `SourcingProvider` interface exists in `lib/provider.ts` declaring at minimum: `id`,
  `label`, `supervisionPolicy: readonly string[]`, and `agentOperatesBrowser: boolean`.
  It contains types and descriptors only — no transport, no browser, no I/O.'
- '`ManualProvider` is the only implementation and declares `agentOperatesBrowser: false`.
  A test asserts its `supervisionPolicy` is byte-identical to today''s `SUPERVISION_POLICY`,
  so the existing attestation is provably unchanged.'
- '`SUPERVISION_POLICY` as a single exported constant is gone; the gate reads policy text
  from the session''s provider. `startSession` still refuses without a named operator, an
  acknowledgement, and an open req — the three existing refusal tests pass unmodified.'
- '`recordManualCapture`''s origin check is provider-agnostic and no longer string-matches
  `supervised-linkedin`. Reuse via `{ reusedFromPool: true }` still bypasses it (ADR-012),
  and a new candidate with a foreign origin is still refused.'
- '`SessionStatus` includes `halted`, distinct from `paused`. `SessionHalt` records `at`,
  `raisedBy` (`operator` | `agent`), `reason`, and `detail`.'
- 'Resuming from `halted` requires a fresh acknowledgement; resuming from `paused` keeps
  today''s behaviour. Both paths are tested, including the refusal when acknowledgement is absent.'
- '`SourcingSession.lastOperatorConfirmationAt` exists with a single exported presence
  window constant (15 minutes) and a pure `operatorPresent(session, now)` predicate. Nothing
  enforces it yet — Increment 6 does — but the predicate is tested at, before, and after the boundary.'
- 'Decision attribution: captures and skips record `decidedBy` and `proposedBy`, defaulting
  to the operator for every existing path.'
- '`PROHIBITED_CAPABILITIES` is unchanged and `supportsCapability()` still returns false for
  every entry. No browser automation, URL, selector, or credential handling is introduced anywhere.'
- 'NO user-visible behaviour change. Every existing sourcingBOT test passes; any test edit is
  a mechanical rename only, and the diff is reviewed on that basis.'
- 'Full validation green: sourcingBOT `vitest`, TypeScript strict, production build, MondayOS
  `pytest`, and Cue App tests with zero Cue changes.'
approval_required: human-review
assigned_to: null
blocked_by: null
child_task_ids: []
commit_refs: []
context: ''
created: '2026-08-13T22:41:17Z'
created_by: human:cli
id: TASK-0059
knowledge_refs: []
metadata: {}
objective: 'Introduce a provider-neutral sourcing boundary so the domain model no longer hard-codes LinkedIn or any browser.
  Rename lib/linkedin.ts to lib/sourcing-session.ts; rename CandidateOrigin ''supervised-linkedin'' to ''supervised-session''
  with backward-compatible load mapping; add SourcingSession.providerId as the durable channel attribution; add a SourcingProvider
  interface with ManualProvider as the only implementation; move SUPERVISION_POLICY from a single constant to per-provider
  policy text; add the halted SessionStatus distinct from paused, SessionHalt records, and the operator-presence fields. Per
  ADR-015, ADR-016 and ADR-017. Ship with NO user-visible behaviour change: ManualProvider must reproduce today''s behaviour
  and today''s policy text exactly. Preserve Candidate versus ReqCandidate separation, the supervision gate, and every entry
  in PROHIBITED_CAPABILITIES.'
parent_task_id: null
priority: P1
status: backlog
status_history:
- changed_at: '2026-08-13T22:41:17Z'
  changed_by: human:cli
  from_status: null
  reason: created
  to_status: backlog
task_type: refactor
title: 'sourcingBOT Increment 4: Provider boundary, domain rename, halted/presence model'
updated: '2026-08-13T22:41:17Z'
work_log: []
---
